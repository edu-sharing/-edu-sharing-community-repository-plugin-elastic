{{- if .Values.global.cluster.istio.enabled }}
{{- if .Capabilities.APIVersions.Has "networking.istio.io/v1beta1" }}
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ include "edusharing_common_lib.names.name" . }}
  labels: {{ include "edusharing_common_lib.labels.app" . | nindent 4 }}
spec:
  hosts:
  - {{ include "edusharing_common_lib.names.name" . }}
  http:
  - route:
    - destination:
        host: {{ include "edusharing_common_lib.names.name" . }}
      weight: 100
  tcp:
  - route:
    - destination:
        host: {{ include "edusharing_common_lib.names.name" . }}
      weight: 100
{{- if .Values.multistage.enabled }}
{{ $stage := printf "%s-stage" (include "edusharing_common_lib.names.name" .) }}
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ $stage }}
  labels: {{ include "edusharing_common_lib.labels.app" . | nindent 4 }}
    app.kubernetes.io/stage: ""
spec:
  hosts:
    - {{ $stage }}
  http:
    - route:
        - destination:
            host: {{ $stage }}
          weight: 100
  tcp:
    - route:
        - destination:
            host: {{ $stage }}
          weight: 100
{{- range .Values.multistage.stages }}
{{ $stage := printf "%s-stage-%s" (include "edusharing_common_lib.names.name" $) .name }}
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ $stage }}
  labels: {{ include "edusharing_common_lib.labels.app" $ | nindent 4 }}
    app.kubernetes.io/stage: {{ .name }}
spec:
  hosts:
    - {{ $stage }}
  http:
    - route:
        - destination:
            host: {{ $stage }}
          weight: 100
  tcp:
    - route:
        - destination:
            host: {{ $stage }}
          weight: 100
{{- end }}
{{- end }}
{{- end }}
{{- end }}
